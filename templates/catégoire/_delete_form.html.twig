<form method="post" action="{{ path('app_cat_goire_delete', {'id': cat_goire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cat_goire.id) }}">
    <button class="btn">Delete</button>
</form>
