{% extends 'base.html.twig' %}

{% block title %}Catégoire{% endblock %}

{% block body %}
    <h1>Catégoire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cat_goire.id }}</td>
            </tr>
            <tr>
                <th>Désingnation</th>
                <td>{{ cat_goire.désingnation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cat_goire_index') }}">back to list</a>

    <a href="{{ path('app_cat_goire_edit', {'id': cat_goire.id}) }}">edit</a>

    {{ include('catégoire/_delete_form.html.twig') }}
{% endblock %}
